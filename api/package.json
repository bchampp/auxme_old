{
  "name": "server",
  "version": "1.0.0",
  "description": "The REST API for AuxMe. Will have spotify interfacing, database access, and queuing capabilities. To be called via POST request from the React client.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrew Farley",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.5",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "querystring": "~0.2.0",
    "request": "~2.83.0"
  }
}
